apiVersion: v2
dependencies:
  - name: cni
    repository: https://istio-release.storage.googleapis.com/charts
    version: "*.*.*"
description: |
  This chart installs istio
name: istio-cni
version: "0"
